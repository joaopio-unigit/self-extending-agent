[project]
name = "evo"
version = "0.1.0"
description = "An experimental self-extending agent that can grow its capabilities by creating and connecting new MCP servers at runtime"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.117.0",
    "httpx>=0.28.1",
    "mcp[cli]>=1.28.1",
    "python-dotenv>=1.2.2",
]
